public class Typecast_class {
    public static void main(String[] args) {
        int num1, num2; //here int is used instead of float because the data type int is less complex than float
        float result;
        num1= 5;
        num2= 2;
        result= (float) num1/num2; //so we need the result in float so we use (float0
        System.out.println(result);
    }
}
